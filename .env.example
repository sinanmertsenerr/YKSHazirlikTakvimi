# Private annual topic-classifier Worker endpoint. Include /v1/classify.
CF_CLASSIFIER_ENDPOINT=

# Same random 32+ character secret configured as CLASSIFIER_AUTH_TOKEN on the Worker.
CF_CLASSIFIER_TOKEN=
