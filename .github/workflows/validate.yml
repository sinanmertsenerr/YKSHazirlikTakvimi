name: Validate application and content

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run audit:production
      - run: npx expo prebuild --platform android --no-install --clean
      - run: npm run validate:native-config
      - run: npm run validate:annual-publication
      - run: npm run validate:ogm-topics
      # Gates the COMMITTED bundled pack (what EAS ships) BEFORE build:pack regenerates
      # assets/pack — a schema change merged without the regenerated pack fails here.
      - run: npm run validate:bundled
      - run: npm run build:pack
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run test:coverage
      - run: npm run validate:cloudflare
      - run: npx expo install --check
      - run: npm run doctor
